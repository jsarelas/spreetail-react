/*
 * HomeSamplePage Messages
 *
 * This contains all the text for the HomeSamplePage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HomeSamplePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeSamplePage container!',
  },
});
